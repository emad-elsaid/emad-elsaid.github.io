I'm using (KDE) Konsole terminal as 

+ It has good support for Arabic text
+ Can customize its colors
+ Can hide menu bar and scrollbar

# Configuration

+ konsolerc in `~/.config/konsolerc`
+ profiles in `~/.local/share/konsole`

